import { createTheme } from "@mui/material";

const theme = createTheme({
    palette: {
        primary: {
            main: "#E13060",
            contrastText: "#FFF",
        }
    }
});

export default theme;
